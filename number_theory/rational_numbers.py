import math




def find_period_decimal(numberator = 22, denominator = 3):
    pass
    # all rational number have a periodic decimal, so this function finds the length, starting index, and the repeating decimal values

